using System.Collections.Generic;
using System.Linq;

namespace CoreData.Common.Extensions
{
    public static class EnumerableExtensions
    {
        //public static bool IsNullOrEmpty<T>(this IEnumerable<T> ctx) =>
        //    ctx == null || !ctx.Any();

        //public static void ForEach<T>(this IEnumerable<T> ctx)
        //{
        //    if (ctx. is List<T>)
        //}
    }
}
